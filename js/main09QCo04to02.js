// Conditionals: Ternary Operator

// syntax:
// condition ? ifTrue : ifFalse;

let soup
let response = 
    soup 
        ? "Yes, we have soup." 
        : "Sorry, no soup today.";
console.log(response);